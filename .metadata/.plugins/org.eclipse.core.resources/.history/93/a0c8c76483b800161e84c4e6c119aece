var connection = require('./mod_mysqlConn');

var OpStatus = function(status) {
	this.status = status;

	this.getStatus = function() {
		var st;
		var sql = 'SELECT * FROM T000MCL WHERE PAGE_ID = ?;';
		connection.query(sql,[status] , function(err, result, fields) {
			st = result[0].OP_STATUS;
			console.log(err)
		})
		connection.end();
		console.log(st)
		return st;
	}
}

module.exports = OpStatus;